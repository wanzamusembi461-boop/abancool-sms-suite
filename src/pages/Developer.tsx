import { PageStub } from "@/components/PageStub";
export default function Developer() {
  return <PageStub title="Developer" description="Create API keys, browse docs, monitor usage and inspect logs." comingIn="Session 6 — API key generation, scoped tokens, rate-limited REST endpoints, and request logs." />;
}
