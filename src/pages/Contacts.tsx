import { PageStub } from "@/components/PageStub";

export default function Contacts() {
  return (
    <PageStub
      title="Contacts"
      description="Manage your address book — groups, tags, imports and exports."
      comingIn="Session 3 — CRUD, CSV import with duplicate detection, groups, tags and search."
    />
  );
}
